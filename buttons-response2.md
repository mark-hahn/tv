
# second response to buttons plan

## answers
8. in usb Sel show inference you said "If only files are selected (no top-level folder active)" i meant files at root of ~/files, some files are not in a folder, this has nothing to do with a selection

9. qbt Force — should do file deletion first

10. you choose which server owns the endpoint.

11. place First after From in local

12.  if `selectedName` is set but `selectedFiles` is empty, select all direct-child files of `selectedName`.

## Instructions
- implement plan if everything is resolved -- otherwise update plan again
- notify me of any more contradictions, ambiguity, or impossible actions
- feel free to make more suggestions
