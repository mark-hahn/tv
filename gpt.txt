
i need a function gatActorPage that is called by the client app. It is passed an actor's name.

The function should do an imdb search for that name, then look through the results and find an actor with that exact name.  then it should get the url for that actor's personal imdb page and then return that url to the client.

If no match is found then return the wikipedia url
  https://en.wikipedia.org/wiki/<actor_name> where <actor_name> is the actor name with spaces replaced with underscores.