
add new command to send to server called setTorrents.  

when torrents are loaded don't show them immediately.  send all torrent data to the server with setTorrents command.  

setTorrents will return a new list with a subset of the torrents meant to be displayed in order received.

each torrent received will have a field called torrentId.

add new command to send to server called getTorrent.  

when torrent card is clicked call getTorrent with torrentId.  

if result of either command has json field "error":<message> then show the error message in torrents pane.

both commands will expect and return json data
