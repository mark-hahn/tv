
=== copilot should ignore this file 
=== do not use this file for prompts
=== it is just a scratchpad for editing copilot prompts.

cd /root/dev/apps/tv-series-client/torrents

is it better to run this msys2 than a wsl-based rsync because it doesn't require file system emulation?

convert this bash script to a windows script and save it in misc, do not use wsl use native windows sw:

nohup rsync -avx \
            --exclude Downloads     --exclude "arduino ide"    \
            --exclude assem         --exclude node_modules     \
            --exclude AppData                                  \
            --exclude *.img         --exclude temp             \
            --exclude .vscode       --exclude Documents        \
            --exclude='*cache*'     --exclude '*Cache*'        \
            --exclude='*google*'    --exclude '*Google*'       \
            --exclude='*Microsoft*' --exclude '*microsoft*'    \
            --exclude='*Packages*'  --exclude '*packages*'     \
            --exclude='*Temp*'      --exclude '*temp*'         \
            --exclude .vscode       --exclude .cache           \
      /mnt/c/Users/mark/  hahnca.com:/mnt/media/backup/mark    \
                        >> /mnt/c/Users/mark/bkup.log &

nohup rsync -av   /mnt/c/Users/mark/Appdata/Roaming/DeskSoft   \
  hahnca.com:/mnt/media/backup/mark/AppData/Roaming/           \
          >> /mnt/c/Users/mark/bkup.log &
