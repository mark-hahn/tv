
rename the Torrents buttons in series.vue and map.view to Download

create a new file src/components/dlstatus.vue. it is a pane component the same size and position as series.vue, map.vue, torrents.vue, etc. 

like the torrents pane it is only used in the app's non-simple mode.

there is a sticky non-scrolling header that has a row of buttons aligned to the right.  The buttons are Torrents, History, Series, and Map


export a function spaceAvail() in torrents/src/usb.js with no args that returns four integers:
total size of usb server storage (usbSpaceTotal),
used size of usb server storage (usbSpaceUsed),
total size of local mounted /mnt/media disk (mediaSpaceTotal),
used size of local mounted /mnt/media disk (mediaSpaceUsed).

add an endpoint to torrents/src/server.js to call spaceAvail() from the client app.

for now return usbSpaceTotal as 2e9.

usbSpaceUsed will be determined by using ssh to the QB_HOST specified in torrents/src/qb-cred.js.  use the command: ssh <QB_HOST> "cd; du -s"
 and return the integer in the last line of the result.

mediaSpaceTotal and mediaSpaceUsed should be gotten from the df command on local mnt/media disk.

----

in torrents.vue add a second status line to the torrents pane sticky header. it will show "Space Available, Seed Box: <usbPercent>, Server: <srvrPercent>". The line will be generated when torrents pane is first shown and updated when any download finishes.

usbPercent is (usbSpaceTotal - usbSpaceUsed) / usbSpaceTotal shown as integer percentage.

srvrPercent is (mediaSpaceTotal - mediaSpaceUsed) / mediaSpaceTotal shown as integer percentage.


add new line below space line "Available, Seed Box: <usb> GB, Server: <srvr> GB" where usb is integer (usbSpaceTotal - usbSpaceUsed)/1e9 and srvr is integer (mediaSpaceTotal - mediaSpaceUsed)/1e9 

switch server space reporting to use /mnt/m-bkup, not /mnt/media