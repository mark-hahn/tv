
in the actors pane rename the clear button to "Regular" and move that button to the far left. then rename the Select button to "Guest" and move it to the immediate right of the Regular button.  Place the input boxes to the right of the buttons.

To the left of the Series button show the message "Series Regulars" when in regular mode and show "Guest Stars" when in guest mode.

When the pane first opens put it in regular mode and only show guest actors when Guest is clicked and it is in guest mode.


increase the Regular/Guest message font-size to 16px.
Create a new middle line in the hdr. move the Regular/Guest messages to the far left of that middle line. move the arrow buttons to the far right of the middle line. Put a Torrents button to the left of the series button and wire it to the torrents pane.

Change button Regular to Regulars and button Guest to Guests. Always show the arrow buttons and if they are clicked in regular mode switch to guest and change the episode as they always do. the Regular/Guest messages should be centered in the space to the left of the arrows. the Regular/Guest messages should be 18px and in bold.

move the Regular/Guest messages to the far left of the middle line but line up the left side with the left side of the show name above.  Move the arrow keys to the center of the middle line.  move the torrents button to the far right of the middle line.

in simple mode when there is no torrents button show the arrows at the far right. 

in actors pane put the show name in a div with a margin-right of 10pix so it is 10px from the series button.  right now it runs into the series button.
use v-show on the actors pane and don't change it when going to series or map panes and back.  reset it only when show selection changes.

in the series pane set the email input box to 14px tall, set the email input box padding to 2px, remove the the margin-top from #infobox, and change the gap in #header to 10px and remove the margin from #header;

in the actors pane the show name is still cut off in the right. word-wrap it.


 when the app is loading, the email input box and an empty infobox are showing.  don't show them until everything else in the series pane shows.

in the actors pane when entering a number in an input box, pressing enter does nothing.  it should do the same thing as clicking on the guest button

 in simple mode when the app is loading, the email input box and an empty infobox are still showing. it works correctly in non-simple mode.

 in simple mode in the actors pane clicking outside the buttons still goes to the series pane. it works corrently in non-simple mode.

 in the torrents pane move the download button to the top line to the left of the series button.