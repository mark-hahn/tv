
refactor match choosing logic that compares a title arg against an array of titles arg and returns best match. this is the algorithm that uses basic normalization first, then aggressive.

call it smartTitleMatch(title, titleArray) that returns chosenName

