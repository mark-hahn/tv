
in the torrents pane, torrent titles that contained text like (S1-S2) were shown as a range of seasons like 1-2: at the left of the second line. 

now include these text formats to detect as season ranges: (S1-S2) (S1-2) where the numbers can be 1 or 2 digits and they may have leading zeros. the S should be case-insensitive.

when sorting the torrents the highest priority sorting should be the season/episode label at the left of the second line, like 1:


Add a button called Force between search and cookies in the torrents pane. The force button should do no filtering of torrents.

When the search button is pressed it should use all the filters that were used in the past like the needed list. 

before any search is done display the message: "Click on Search to find torrents for <show name>." instead of "No torrents found." show "No torrents found." after a search found no torrents.