
explain with no changes

this:

    const loginLink = page.locator('a.login.trackable-link[' +
        'data-trackable-name="Login"][href="/account/login"]');
    await loginLink.first().waitFor({ state: 'visible', timeout: 5000 });

is not locating this

<a class="login trackable-link" data-trackable-name="Login" href="/account/login">Login</a>

would it work to use this for a delay until page is loaded?
loop every 100ms and do 
  const html = await page.content();
until the html includes some expected string in the page?