
in non-simple mode:
  center items in hdrtop just they as they are in simple mode but don't change the count displays in either mode

in simple mode:

in all modes:
  when starting a web search, show modal dialog with no buttons (is that a toast?) that says Searching web for information about show: <show name>. Please wait ...

  Then remove the dialog when the call to the server returns.
