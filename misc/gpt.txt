
Torrents, Status, History, Series, and Map

do not reset torrents or status panes except when show selection changes.

the torrent search in torrent pane restarts when switching panes.  it should only restart when a show selection changes

the status pane should never reset
