
## in cycle

- do not abort Between Files

- If a startProc request comes in while processing set a cycleRestartNeeded flag and finish the cycle. do not restart the cycle immediately Between Files

- when cycle finishes if cycleRestartNeeded is true then clear cycleRestartNeeded and restart the cycle. 

- is this code valid:
      type = 'episode';
        if (!type === 'episode') {
          log('\nskipping non-episode:', fname);
          badFile('non-episode');
          return;
        }

