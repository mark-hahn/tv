
# answers to movies plan
1. remove eztv
2. Stream should be hidden in movie mode
3. Movie (exit movie) button should always be visible
4. Implement as "Search Movies"
5. bypass the cache in movie mode -- clear tv show cache
6. remove eztv
7. IPTorrents and TorrentLeech have movie categories in their web pages, do one-time experiments and stop and tell me if they fail
8. verify spelling before implementing
9. it should be triggered immediately
10. the selected input should appear at the far left 

# responses to suggestions
- do not Persist movieMode across page refresh
- indicators of movie mode
  - in tor pane:
    - the `Exit Movie` button should be highlighted light-red in movie mode
    - change title at left of row 1 of header to `Tor Movie Search` 
      - do not show search text
  - in qbt pane there is already a message to left of row2 of hdr for this
- there should be no Movie search history
