


- when a card is clicked and the "more" property is true 
  - call getRemainingReview(<reviewId>)
  - getRemainingReview(<reviewId>) will return a string
  - remove the elipsies ... from the matching card's text and append the getRemainingReview returned string to that text
  - set "more" to false in the object backing the card that was clicked

 -add a new reviews tab pane in the client
- it should be in a new component file /root/apps/tv/apps/client/src/components/reviews.vue
- add a new tab and tab pane called "Reviews" 
- it should show in both simple and non-simple modes
- it should be to the immediate right of the actors button
- it should have a two-row header
- the top row far left should be the selected show title in the same style as the title in the actors tab pane 
- the top row far right should have the Rotten button for the selected show (same button as in the series tab pane)
- the second row far left should have four radio buttons with these labels
    - "All Critics"
    - "Top critics"
    - "All Audience"
    - "Verified Audience"
- the selected button should have a light-gray background
- when first loaded the "All Critics" button should be selected
- below the header should be two scrolling panes of cards
    - they should be two vertical columns, side-by-side
    - they should be full-height from bottom of header to bottom of pane
- each card should be the full width of the column
- get reviews by calling getReviews(<url>, <button name>) to the api server
    - <url> is the url from the Rotten button for the selected show
    -  <button name> is the name of the selected button
- getReviews will return a json array of review objects. each object will have these properties
    - "reviewId":<number>
    - "sortIdx":<number>
    - "author":"<string>"
    - "publication":"<string>"
    - "numStars":<number>
    - "ratings":"<string>"
    - "text":"<string>"
    - "more":< boolean>
- call getReviews when pane is loaded, or when one of the four buttons are clicked
- each review entry is a card
    - reviews should be sorted by sortIdx.
    - the left and right columns each have the  same list of cards
    - the left column is sorted by sortIdx ascending
    - the right column is sorted by sortIdx descending
- the card format is:
    - the left side of the top row is "<author> (<publication>)" or just author if no publication.
    - the right side of the top row is the ratings display
        - the ratings display is the "ratings" property string if non-empty
        - if "ratings" property is empty then the ratings display is a row of 4 stars
            - the row of stars is a standard ratings display of 0 to 4 representing the "numStars" property
                - there are three Font Awesome star icons
                    - fa-solid      fa-star                     Full star    
                    - fa-solid      fa-star-half-stroke  Half star     
                    - fa-regular  fa-star                     Empty star 
    - the rest of the lines show the "text" property
    - put a 1px line between the header and the text, the full width of the card
    - if the "more" property is true then append ellipsis ... to the text
- when a card is clicked and the "more" property is true call getRemainingReview(<reviewId>)
- for now we will just have a dummy test pane
    - the api calls getReviews() or getRemainingReview() should be commented out
    - you should generate a hard-wired list of 6 dummy entries to use for the test
