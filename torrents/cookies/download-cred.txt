# torrents/cookies/download-cred.txt
#
# SFTP settings used by torrents/src/download.js to upload a .torrent file
# to your remote qBittorrent "watch" folder.
#
# Lines are KEY=VALUE. Quotes are optional.

# Option A (recommended):
SSH_TARGET=xobtlu@oracle.usbx.me
SFTP_PASS=90-TYUrtyasd

# Option B:
#SFTP_HOST=host
#SFTP_USER=user
#SFTP_PASS=your_password

# Optional (defaults to 22):
#SFTP_PORT=22

# Optional. If omitted, defaults to /home/<user>/watch/qbittorrent
#REMOTE_WATCH_DIR=/home/user/watch/qbittorrent
